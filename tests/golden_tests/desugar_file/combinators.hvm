(foo) = λa λb λc (foo a)

(bar) = λa λb (a bar b)

(List.ignore list ignore) =
  match list {
    (List.cons): (List.ignore list.tail (List.ignore))
    (List.nil): 0
  }

(baz) = {0 1 2 3 λa a foo}

(qux) = {0 qux}

(list) = [0 list]

(Main) = list
