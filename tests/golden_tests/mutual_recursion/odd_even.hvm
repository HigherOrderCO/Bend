data Bool = True | False

(_if 0 then else) = else
(_if _ then else) = then

(isOdd  n) = (_if (== n 0) Bool/False (isEven (- n 1)))
(isEven n) = (_if (== n 0) Bool/True  (isOdd  (- n 1)))

(Main) = (isOdd 4)
