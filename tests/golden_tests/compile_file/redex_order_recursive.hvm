tail_recursive = @x (x @pred @acc (tail_recursive pred (+ 1 acc)) @acc 0)

fold = @bm (bm
  @lft @rgt (add (fold lft) (fold rgt))
  @s @z (s @s @z z)
)

add = @a (a 
  @p @b (add p @s @z (s b))
  @b b
)

main = *